## Блок 1: Межсервисное взаимодействие (18 баллов)

### Обязательные пункты (0 баллов)
- [+] Реализовано минимум **3 микросервиса**.
- [+] **REST API** реализован минимум в 2 сервисах с корректным использованием HTTP методов (GET, POST, PUT, DELETE).
- [+] Реализован **синхронный вызов** между сервисами (HTTP/gRPC).
- [+] Корректная **обработка ошибок** с использованием HTTP статус-кодов (200, 400, 404, 500).
- [+] Наличие **README.md** с пошаговой инструкцией по запуску проекта.

### Дополнительные пункты (нужно набрать минимум 6 баллов, макс. 12)
- [+] **API Gateway** (например, Nginx, YARP, Kong) с маршрутизацией между сервисами. *Все запросы идут через единый шлюз.* **(5 баллов)**
- [+] Использование **Message Broker** (RabbitMQ, Kafka) для асинхронной коммуникации (минимум один сценарий с Producer и Consumer). **(5 баллов)**
- [+] Реализация **Event-Driven паттерна** (публикация событий в брокер с несколькими подписчиками на одно событие). **(4 балла)**

---

## Блок 2: Данные и согласованность (consistency) (17 баллов)

### Обязательные пункты (0 баллов)
- [+] Реализован принцип **Database per Service** (каждый сервис работает с собственной базой данных).
- [+] Используются **миграции БД** (Flyway, Liquibase, EF Migrations, Alembic). Схема данных версионируется и применяется автоматически при старте.
- [+] Реализована **событийная согласованность (Eventual Consistency)** хотя бы в одном сценарии (как осознанный архитектурный выбор).

### Дополнительные пункты (нужно набрать минимум 6 баллов, макс. ~12)
- [+] Применение паттерна **CQRS** (разделение моделей чтения и записи, оптимизация read-модели). **(5 баллов)**
- [+] Реализация паттерна **Inbox** для обеспечения идемпотентности обработки входящих сообщений (сохранение ID сообщений для игнорирования дубликатов). **(4 балла)**
- [+] Использование **распределенного кеша (Redis)** для кеширования часто запрашиваемых данных (паттерны Cache-aside или Read-through). **(4 балла)**

---

## Блок 3: Отказоустойчивость и Наблюдаемость (18 баллов)

### Обязательные пункты (0 баллов)
- [+] **Структурированное логирование:** вывод логов в JSON формате (содержат timestamp, уровень, имя сервиса) в stdout.
- [+] Наличие **endpoint'ов для проверки здоровья** (`/health` или `/healthz`), возвращающих 200 OK.
- [+] Реализован **Graceful Shutdown** (сервис корректно завершает текущие запросы и закрывает соединения при остановке).
- [+] Проброс **Correlation ID** между сервисами (ID запроса передается по цепочке и логируется во всех сервисах).

### Дополнительные пункты (нужно набрать минимум 6 баллов, макс. 12)
- [+] Настройка **централизованного сбора логов** (стек ELK, Loki + Promtail, Fluentd) для поиска по correlation ID. **(5 баллов)**
- [+] Интеграция **метрик Prometheus** и создание **дашборда в Grafana** с ключевыми метриками сервисов. **(5 баллов)**
- [+] Настройка **таймаутов (Timeouts)** для всех внешних вызовов. **(3 балла)**

---

## Блок 4: Безопасность и Продакшн-готовность (17 баллов)

### Обязательные пункты (0 баллов)
- [+] Использование **переменных окружения** для всей конфигурации (строки подключения к БД, порты, URL). Отсутствие захардкоженных секретов.
- [+] Наличие **Docker Compose** файла для локального запуска всей системы одной командой (`docker-compose up`).
- [+] Файлы `.env` **не хранятся в Git** (добавлены в `.gitignore`). В репозитории присутствует только файл-пример `.env.example`.
- [+] Наличие автоматически генерируемой **документации API** (Swagger/OpenAPI) с возможностью тестирования эндпоинтов через UI.

### Дополнительные пункты (нужно набрать минимум 6 баллов, макс. 12)
- [+] Реализация **JWT аутентификации и авторизации** (логин, выдача токена, проверка токена, разделение ролей user/admin). **(5 баллов)**
- [+] Настройка **CI/CD пайплайна** (GitHub Actions, GitLab CI) для автоматической сборки, тестирования и деплоя. **(5 баллов)**
- [+] Использование **Feature Flags** для включения/выключения функций без передеплоя. **(4 балла)**

