# ==================== User Service DB ====================
USER_DB_USER=postgres
USER_DB_PASSWORD=changeme
USER_DB_NAME=userdb

# ==================== JWT (User Service выпускает токены) ====================
JWT_SECRET=replace-with-a-real-random-secret-at-least-32-characters-long

# ==================== RabbitMQ (общий брокер для всех сервисов) ====================
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# ==================== Будущие сервисы (пока не используются) ====================
# CONTENT_DB_USER=postgres
# CONTENT_DB_PASSWORD=changeme
# CONTENT_DB_NAME=contentdb
# MINIO_USER=minioadmin
# MINIO_PASSWORD=changeme
# SOCIAL_DB_USER=mongoadmin
# SOCIAL_DB_PASSWORD=changeme
